'''
Created on Apr 25, 2017

@author: iagmon
'''
import csv
from controllers.CLIUtils.Step import Step
import errno
import os
class CsvFileParser(object):
    '''
    classdocs
    '''


    def __init__(self,csvFileName):
        '''
        Constructor
        '''
        self.csvFileName = csvFileName
        
    def initializeTestDefinition(self):
        steps = []
        steps2 = []
        try:
            with open(self.csvFileName+".csv") as csvfile:
                reader = csv.DictReader(csvfile)
                num_cols = len(reader.next())
                print num_cols
                for index in xrange(num_cols):                  
                    for row in reader:     
                        print 'jsonFileName'+str(index)    
                        steps.append(Step(row['jsonFileName'+str(index)]))
                print steps
        except:
            raise IOError("the file " + self.csvFileName + " not found")
        
    '''def get_Number_Of_Cols(self):
        i = 1
        with open(self.csvFileName+".csv") as csvfile:
            reader = csv.DictReader(csvfile)
            for row in reader:  
                print row'''
            

        
            



