# conftest.py
import pytest
import xml.etree.ElementTree as ET

def add(this,that):
    result = this + that
    return result


def test_add():
    assert add(10,15) == 25
    
def step():
    import json
    from pprint import pprint
    tree = ET.parse("confOfResp.xml")
    xml=ET.fromstring(contents))
    with open(xml.find('./bar').attrib['key'].text+'data.txt') as data_file:    
        data = json.load(data_file)
        pprint(data)

step()