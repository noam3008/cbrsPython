from flask import Flask,request,jsonify,g
from controllers.ENodeBController import ENodeBController
from controllers.MController import Mcontroller
import time
import threading
from Queue import Queue
app = Flask(__name__)

enodeBController = ENodeBController(None)
@app.route('/cbsd/<typeOfCalling>',methods=['POST'])
def sentFlaskReqToServer(typeOfCalling):
    #q = Queue()
    #threading.Thread(target=enodeBController.run, kwargs={'jsonReq': request.get_json(),'q':q}).start()
    #return jsonify(q.get())
    return jsonify(enodeBController.run(json.request)

def runFlaskServer():
    app.run()
    



